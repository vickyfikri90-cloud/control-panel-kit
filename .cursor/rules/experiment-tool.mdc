---
description: Rules for adapting the Experiment Tool
globs:
  - "**/*"
alwaysApply: true
---

# Experiment Tool

Before modifying this project or copying it to a new experiment:

1. Read the **experiment-tool** skill (`.cursor/skills/experiment-tool/SKILL.md`)
2. Read `AGENTS.md` — conventions, do/don't, wiring pattern
3. Read `docs/COMPONENTS.md` — per-component API and rules
4. Read `README.md` — quick start and architecture

Key rules:
- Single `applyAll()` loop for all panel → preview + snippet sync
- Init functions: `window.initXxx(root, options)` returning controller object
- Keep 241px panel, 240px fields, 24px control height
- Sync `component.html` ↔ `components-templates.js`
- Don't edit build outputs (`button-hover.html`) directly
